import { Box, Typography } from '@mui/material';

const History = () => (
    <Box
        sx={{
            height: '500px',
            backgroundColor: '#9fc9e2',
            padding: '40px',
        }}
    >
        <Typography variant="h4">История</Typography>
    </Box>
);

export default History;
